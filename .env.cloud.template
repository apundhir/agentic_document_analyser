# Cloud Configuration Template (Unified - Easy Billing) -> Rename to .env

# Single Provider: Fireworks AI
# ONE API Key, ONE Bill.
# We will use Qwen2.5-VL-72B for EVERYTHING (Layout, OCR, Reasoning).

# 1. Unified Endpoint
# Sign up: https://fireworks.ai/
FIREWORKS_API_KEY=fw_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
FIREWORKS_MODEL=accounts/fireworks/models/qwen2p5-vl-72b-instruct

# 2. Service-Specific overrides (Optional, defaults to above)
# We map all internal services to this single endpoint
VLM_ENDPOINT=https://api.fireworks.ai/inference/v1/chat/completions
VLM_API_KEY=${FIREWORKS_API_KEY}
VLM_MODEL=${FIREWORKS_MODEL}

LAYOUT_ENDPOINT=https://api.fireworks.ai/inference/v1/chat/completions
LAYOUT_API_KEY=${FIREWORKS_API_KEY}
LAYOUT_MODEL=${FIREWORKS_MODEL}

OCR_ENDPOINT=https://api.fireworks.ai/inference/v1/chat/completions
OCR_API_KEY=${FIREWORKS_API_KEY}
OCR_MODEL=${FIREWORKS_MODEL}

# Infrastructure
ENV=prod
LOG_LEVEL=INFO
